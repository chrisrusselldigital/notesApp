var repeat = require("./repeat").repeat;
var exclaim = require("./exclaim").exclaim;

if(exlaim(repeat, "hi") !== "hi!!!!!") {
    throw new Error("Exclaiming hi should be equal hi!!!!!");
} else {
    console.log(".");
}
